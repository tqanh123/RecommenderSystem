Phương pháp tổng thể này được gọi là: Hybrid Cascade Recommender System with Data Denoising (Hệ thống Gợi ý Lai ghép Phân tầng có Khử nhiễu).Nó kết hợp Semi-Supervised Learning (để gán nhãn lại dữ liệu nhiễu), Graph Neural Networks (LightGCN để học sở thích), và Ensemble Learning (Random Forest để xếp hạng).Giai đoạn 1: Data Preprocessing & Denoising (Tiền xử lý & Khử nhiễu)Mục tiêu: Biến dữ liệu thô nhiều rác (click nhầm) thành dữ liệu sạch để model học đúng.Phân tầng dữ liệu (3-Tier Classification):Dữ liệu gốc được chia thành 3 nhóm dựa trên hành vi:🟢 Tầng 1 (Strong Positive): Có tương tác mạnh (Like, Share, Follow). Đây là "Sự thật" (Ground Truth) chắc chắn nhất.🔴 Tầng 3 (Strong Negative): Xem cực ngắn ($\le$ 2s) VÀ Không tương tác. Đây là hành vi "Lướt qua" (Scroll).🟡 Tầng 2 (Ambiguous - Vùng xám): Các trường hợp còn lại (Ví dụ: Click nhưng không like, hoặc xem > 2s nhưng im lặng). Đây là vùng cần AI phán xử.Huấn luyện "Teacher Model" (RF Filter):Sử dụng Tầng 1 (Gán nhãn 1) và Tầng 3 (Gán nhãn 0) để huấn luyện một mô hình Random Forest nhỏ.Model này học cách phân biệt hành vi "Thích" và "Ghét" dựa trên Metadata (Category, Age, Gender) và bối cảnh (Watching Time).Khử nhiễu (Pseudo-Labeling):Dùng "Teacher Model" chấm điểm cho Tầng 2 (Vùng xám).Thăng hạng: Nếu điểm > 0.6 $\rightarrow$ Đưa vào tập Positive (coi như user thích).Hạ hạng: Nếu điểm < 0.4 $\rightarrow$ Đưa vào tập Negative Pool (coi như user ghét).Loại bỏ: Các điểm lửng lơ (0.4 - 0.6) bị vứt bỏ để tránh làm nhiễu model chính.Giai đoạn 2: Data Splitting (Phân chia dữ liệu)Mục tiêu: Chia tập Train/Test sao cho không bị rò rỉ dữ liệu tương lai.Tạo tập Positive sạch: Gồm Tầng 1 + Tầng 2 (được thăng hạng).Split Train/Test: Chia tập Positive này theo tỷ lệ (ví dụ 90/10) cho từng user.Train Set: Dùng để xây đồ thị.Test Set: Dùng để kiểm tra kết quả cuối cùng.Xử lý Negative Pool (Quan trọng):Lấy tập Negative (từ Tầng 3 + Tầng 2 bị hạ hạng).Anti-Leakage Filter: Loại bỏ tất cả các cặp (User, Item) nào đang nằm trong Test Set. (Tránh trường hợp hôm nay user lướt qua, nhưng ngày mai user quay lại like, nếu đưa vào negative train thì model sẽ bị mâu thuẫn).Giai đoạn 3: Negative Sampling (Lấy mẫu âm)Mục tiêu: Tạo dữ liệu huấn luyện cân bằng cho LightGCN.Thay vì random 100%, bạn sử dụng chiến lược Hybrid Sampling (50/50):50% True Negatives (Hard Negatives): Lấy từ Negative Pool đã xử lý ở trên. Đây là những item user đã xem nhưng chê. Giúp model học được "khẩu vị" user tốt hơn.50% Random Negatives (Weak Negatives): Lấy ngẫu nhiên những item user chưa bao giờ thấy. Giúp model giữ được tính tổng quát.Giai đoạn 4: Representation Learning (LightGCN)Mục tiêu: Học Vector đặc trưng (Embedding).Xây dựng đồ thị: Dùng Train Set (chỉ chứa Positive sạch) để tạo các cạnh nối User-Item.Training: LightGCN chạy thuật toán lan truyền tin (Message Passing) để tạo ra:User Embedding Vector ($E_u$)Item Embedding Vector ($E_i$)Thể hiện sự tương đồng tiềm ẩn giữa người và vật.Giai đoạn 5: Feature Engineering & Final Ranking (Random Forest)Mục tiêu: Kết hợp Embedding với Metadata để đưa ra quyết định cuối cùng.Tạo bộ dữ liệu cho RF: Lấy lại các cặp (User, Item) từ bước Sampling.Trích xuất đặc trưng (Feature Extraction) - Chuẩn Không Leakage:Embedding: Lấy từ LightGCN (gcn_dot_product, vector thành phần...).Metadata: Age, Gender, Video Category.Item Stats (Tính từ Train): i_avg_time (Thời gian xem trung bình của item đó), i_popularity (Độ hot). Tuyệt đối không dùng watching_times của chính user đó.Huấn luyện Final RF: Model học mối quan hệ phi tuyến tính giữa:Sở thích tiềm ẩn (LightGCN).Đặc tính nội dung (Metadata).Chất lượng nội dung (Item Stats).Inference (Dự đoán): Trả về xác suất User thích Item (predict_proba) để xếp hạng Top K gợi ý.Tóm tắt sơ đồ luồng dữ liệu (Data Flow):Đoạn mãgraph TD
    A[Raw Data] --> B{Phân loại 3 Tầng}
    B -->|Like/Share| C[Strong Pos]
    B -->|Xem ít| D[Strong Neg]
    B -->|Click/Xem nhiều| E[Ambiguous]
    
    C & D --> F[Train RF Teacher]
    F -->|Dự đoán| E
    E -->|Score cao| C_new[Added to Pos]
    E -->|Score thấp| D_new[Added to Neg Pool]
    
    C & C_new --> G[Split Train/Test]
    D & D_new --> H[Negative Pool]
    G -->|Test Set| I[Anti-Leakage Filter]
    H --> I
    I --> J[True Negatives]
    
    G -->|Train Pos| K[LightGCN]
    J -->|50%| L[Sampler]
    Random -->|50%| L
    
    K & L --> M[Feature Engineering]
    M --> N[Final Random Forest]
    N --> O[Top K Recommendations]